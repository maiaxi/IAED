#include<stdio.h>
#include<stdlib.h>
#include<ctype.h>
#include<string.h>
int main(void){
    char command;
    scanf("%s", &command);
    if (command=="a"){
        printf("%s", command);
    }
    return 0;
}